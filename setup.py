from setuptools import setup, find_packages

setup (
    name="m_logger", 
    description="Logger package with multiprocessing support",
    version="1.0.0", 
    author="log.txt", 
    author_email="log.txt@localhost", 
    packages=find_packages()
)